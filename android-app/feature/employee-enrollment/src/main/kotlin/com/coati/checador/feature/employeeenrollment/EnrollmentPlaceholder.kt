package com.coati.checador.feature.employeeenrollment

/**
 * Punto de entrada del módulo feature:employee-enrollment.
 *
 * Exporta la pantalla principal de registro de empleado para que el módulo :app
 * pueda importarla y agregarla al grafo de navegación.
 *
 * Rutas de navegación:
 *   "registro_empleado" → PantallaRegistroEmpleado
 */
object EmployeeEnrollmentNavigation {
    const val RUTA = "registro_empleado"
}
